/* jshint node:true */
// var RSVP = require('rsvp');

// For details on each option run `ember help release`
module.exports = {
  remote: 'origin',
  annotation: 'Release %@',
  message: 'Bumped version to %@',
  publish: true
};
