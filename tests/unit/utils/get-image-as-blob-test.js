import getImageAsBlob from 'dummy/utils/get-image-as-blob';
import { module, test } from 'qunit';

module('Unit | Utility | get image as blob');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getImageAsBlob();
  assert.ok(result);
});
